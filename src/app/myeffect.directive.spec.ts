import { MyeffectDirective } from './myeffect.directive';

describe('MyeffectDirective', () => {
  it('should create an instance', () => {
    const directive = new MyeffectDirective();
    expect(directive).toBeTruthy();
  });
});
