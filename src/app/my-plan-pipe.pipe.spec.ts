import { MyPlanPipePipe } from './my-plan-pipe.pipe';

describe('MyPlanPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyPlanPipePipe();
    expect(pipe).toBeTruthy();
  });
});
